package CampingReg;

/***********************************************************************
 * CamptingRegistration has the main method in it, which runs when the
 * application is executed
 **********************************************************************/
public class CampingRegistration {
	
	/*******************************************************************
	 * The main function runs when the program is first executed
	 * 
	 * @param args are any arguments that could be used in the main
	 * function. These arguments currently have no effect on how the
	 * application runs
	 ******************************************************************/
	public static void main(String[] args) {
		GUICampingReg app = new GUICampingReg();
		app.run();
	}
}
